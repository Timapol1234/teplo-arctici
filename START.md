# 🚀 Быстрый старт - Тепло Арктики

Это руководство поможет вам быстро запустить проект локально.

## Предварительные требования

1. **Node.js** (версия 18 или выше)
   - Скачать: https://nodejs.org/
   - Проверить: `node --version`

2. **SQLite** - НЕ требует установки! Автоматически включен в проект через sqlite3

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

Это установит все необходимые пакеты (~30 секунд).

## Шаг 2: База данных SQLite

**Ничего делать не нужно!** SQLite база данных создается автоматически при первом запуске `npm run db:init`

Файл базы данных будет создан в: `database/teplo_arctici.db`

## Шаг 3: Проверьте файл .env

Файл `.env` уже создан с правильными настройками для SQLite:

```env
# Database (SQLite)
DB_PATH=./database/teplo_arctici.db
```

## Шаг 4: Инициализация базы данных

```bash
npm run db:init
```

Это создаст все таблицы и администратора по умолчанию.

## Шаг 5: Заполнение тестовыми данными (опционально)

```bash
npm run db:seed
```

Это добавит:
- 3 благотворительных сбора
- 20 тестовых пожертвований
- 5 отчетов о расходах

## Шаг 6: Запуск сервера

```bash
npm run dev
```

Сервер запустится на http://localhost:3000

## 🎉 Готово!

Откройте браузер:

### Публичный сайт
- **Главная страница**: http://localhost:3000
- **Отчеты**: http://localhost:3000/reports.html
- **О фонде**: http://localhost:3000/about.html

### Админ-панель
- **Вход**: http://localhost:3000/admin/
- **Email**: `admin@teplo-arctici.ru`
- **Пароль**: `admin123`

После входа доступны:
- **Транзакции**: http://localhost:3000/admin/dashboard.html
- **Сборы**: http://localhost:3000/admin/campaigns.html
- **Отчеты**: http://localhost:3000/admin/reports.html

## Возможные проблемы

### Ошибка компиляции sqlite3

**Проблема**: `node-gyp rebuild` failed

**Решение Windows**:
1. Установите Visual Studio Build Tools: https://visualstudio.microsoft.com/downloads/
2. Выберите "Desktop development with C++"
3. Запустите: `npm install`

**Альтернатива (Windows)**:
```bash
npm install --build-from-source
```

**Решение Linux/Mac**:
```bash
sudo apt install build-essential  # Ubuntu/Debian
# или
xcode-select --install  # macOS
```

### Порт 3000 уже занят

**Проблема**: `Error: listen EADDRINUSE: address already in use :::3000`

**Решение**: Измените порт в `.env`:
```env
PORT=3001
```

### Нужно пересоздать базу данных

**Решение**:
```bash
# Удалить файл базы данных
rm database/teplo_arctici.db  # Linux/Mac
del database\teplo_arctici.db  # Windows

# Пересоздать
npm run db:init && npm run db:seed
```

## Дополнительные команды

```bash
# Остановить сервер
Ctrl + C

# Пересоздать базу данных
rm database/teplo_arctici.db && npm run db:init && npm run db:seed

# Запустить в продакшн режиме
npm start

# Сделать бэкап базы данных
cp database/teplo_arctici.db database/backup_$(date +%Y%m%d).db
```

## Следующие шаги

1. Изучите [CLAUDE.md](./CLAUDE.md) для понимания архитектуры
2. Прочитайте [ADMIN_PANEL.md](./ADMIN_PANEL.md) для работы с админ-панелью
3. Измените пароль администратора в админ-панели
4. Начните создавать свои благотворительные сборы!

## Поддержка

Если возникли проблемы, проверьте:
- Логи сервера в терминале
- Логи браузера (F12 → Console)
- База данных создана: проверьте наличие файла `database/teplo_arctici.db`

## Преимущества SQLite

- ✅ Не нужно устанавливать отдельный сервер БД
- ✅ Вся база в одном файле - легко делать бэкапы
- ✅ Идеально для разработки и тестирования
- ✅ Можно использовать SQLite Browser для просмотра данных: https://sqlitebrowser.org/
