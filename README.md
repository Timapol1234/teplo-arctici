# Тепло Арктики

[![CI](https://github.com/username/teplo-arctici/actions/workflows/ci.yml/badge.svg)](https://github.com/username/teplo-arctici/actions/workflows/ci.yml)

Веб-платформа благотворительного фонда с максимальной прозрачностью для жертвователей.

## Особенности

- 🔴 **Live-лента транзакций** - Все пожертвования отображаются в реальном времени
- 🔒 **Система верификации** - Опциональное SHA-256 хеширование транзакций для дополнительной прозрачности
- 📊 **Детальные отчеты** - Полная отчетность по каждому целевому сбору
- 🛡️ **Безопасность** - Защита от SQL-инъекций, XSS, CSRF атак
- 📱 **Адаптивный дизайн** - Работает на всех устройствах

## Требования

- Node.js 18+
- SQLite (встроен, установка не требуется)
- npm или yarn

## Установка

1. Клонировать репозиторий:
```bash
git clone <repository-url>
cd teplo_arctici
```

2. Установить зависимости:
```bash
npm install
```

3. Инициализировать базу данных SQLite (автоматически создается):
```bash
npm run db:init
```

4. Заполнить тестовыми данными (опционально):
```bash
npm run db:seed
```

## Запуск

### Режим разработки
```bash
npm run dev
```

### Продакшн режим
```bash
npm start
```

Сайт будет доступен по адресу: http://localhost:3000

## Админ-панель

Для доступа к админ-панели:
1. Перейдите по адресу: http://localhost:3000/admin/
2. Войдите с тестовыми учетными данными:
   - **Email:** `admin@teplo-arctici.ru`
   - **Пароль:** `admin123`

### Возможности админ-панели

- **Транзакции** (`/admin/dashboard.html`) - Просмотр и добавление пожертвований
- **Сборы** (`/admin/campaigns.html`) - Управление благотворительными сборами
- **Отчеты** (`/admin/reports.html`) - Создание отчетов о расходах

Подробная документация: [ADMIN_PANEL.md](./ADMIN_PANEL.md)

## Структура проекта

```
teplo_arctici/
├── backend/        # Backend код (Express.js)
├── database/       # SQL схемы и миграции
├── public/         # Frontend файлы
└── desing/         # Дизайн-макеты
```

Подробнее см. [CLAUDE.md](./CLAUDE.md)

## API Endpoints

### Публичные
- `GET /api/donations/recent` - Последние пожертвования
- `GET /api/campaigns` - Активные сборы
- `GET /api/reports/:campaignId` - Отчет по сбору

### Административные
- `POST /api/admin/login` - Авторизация
- `POST /api/admin/donations` - Добавить транзакцию
- `POST /api/admin/campaigns` - Создать сбор
- `POST /api/admin/reports` - Добавить отчет

## Безопасность

- ✅ Защита от SQL-инъекций (параметризованные запросы)
- ✅ Защита от XSS (санирование ввода)
- ✅ Защита от CSRF (токены)
- ✅ Rate Limiting (100 запросов за 15 минут)
- ✅ HTTPS/SSL готов к продакшн

## Лицензия

MIT

## Контакты

Фонд "Тепло Арктики"
